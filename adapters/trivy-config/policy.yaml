adapter_id: trivy-config
display_name: Trivy Configuration Adapter
execution_modes:
  - passive
requires_human_approval: false
requires_target_scope_match: true
supports_target_kinds:
  - repo
  - filesystem
  - image
default_timeout_seconds: 300
evidence_requirements:
  - json_report
  - misconfiguration_summary
  - affected_resource
