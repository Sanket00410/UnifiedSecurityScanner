adapter_id: tfsec
display_name: tfsec Terraform Analyzer
execution_modes:
  - passive
requires_human_approval: false
requires_target_scope_match: true
supports_target_kinds:
  - terraform
  - repo
  - filesystem
default_timeout_seconds: 240
evidence_requirements:
  - json_report
  - policy_match_summary
  - source_location
